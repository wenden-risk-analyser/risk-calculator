const emptyCustomerService = {
    getAll() {}
};

const emptyBetService = {
    getAll() {}
};

const invalidService = {
    forgetIt: 5
};

module.exports = {
    emptyCustomerService,
    emptyBetService,
    invalidService
};
